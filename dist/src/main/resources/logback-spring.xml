<?xml version="1.0" encoding="UTF-8"?>
<configuration>

    <!--
        Two output forms:
          - 'plain' (default, dev-friendly): single line with MDC correlationId.
          - 'json'  (prod): structured JSON via logstash-logback-encoder, one event per line.
        Switch with -Dspring.profiles.active=json or env SPRING_PROFILES_ACTIVE=json.
    -->

    <springProfile name="!json">
        <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
            <encoder>
                <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} %-5level [%X{correlationId:-}] %logger{36} - %msg%n</pattern>
            </encoder>
        </appender>
    </springProfile>

    <springProfile name="json">
        <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
            <encoder class="net.logstash.logback.encoder.LogstashEncoder">
                <includeMdcKeyName>correlationId</includeMdcKeyName>
                <customFields>{"app":"cpayment"}</customFields>
            </encoder>
        </appender>
    </springProfile>

    <root level="INFO">
        <appender-ref ref="CONSOLE"/>
    </root>

    <logger name="com.cpayment" level="INFO"/>
</configuration>
